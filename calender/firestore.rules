rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Replace 'YOUR_USER_ID_HERE' with your actual UID from the Firebase Console -> Authentication tab
      allow read, write: if request.auth != null && (request.auth.uid == 'sQiCltLYU2MJFQBcLjy99T3uXOo2' || request.auth.token.email == 'rDYaEfWr3sStcfLqpg1XCdS4WAj1');
    }
  }
}
